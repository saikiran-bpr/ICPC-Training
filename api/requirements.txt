# Runtime deps for the FastAPI function on Vercel.
# Mirrors backend/pyproject.toml [project.dependencies] minus uvicorn
# (Vercel invokes the ASGI app directly — no server needed).
fastapi>=0.115
python-multipart>=0.0.9
itsdangerous>=2.2
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.4
psycopg[binary]>=3.1
psycopg-pool>=3.2
werkzeug>=3.0
openpyxl>=3.1
python-dotenv>=1.0
